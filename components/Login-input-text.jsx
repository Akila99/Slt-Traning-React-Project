import React from "react";
import "../public/style.css";

function InputTextBox(props) {
  return <input type="text" {...props} />;
}
export default InputTextBox;
// name={props.name} id={props.id} condition={props.inputCondition}
